// change the input string to an array
// reverse the array
// join the array to a string
// return the string
function reverse(str) {
  const arr = str.split("");
  arr.reverse();
  return arr.join("");
}
